---
import { baseUrl } from '@/config/site'
const pages = [
    { name: 'Home', url: `${baseUrl}` },
    { name: 'About', url: `${baseUrl}/resume` },
    { name: 'Repos', url: `${baseUrl}/repos` },
    { name: 'Cuadernos', url: `${baseUrl}/bitacora` }
]
---

<header class="sticky top-0 z-50 w-full border-b bg-sidebar text-sidebar-foreground  print:hidden">
    <div class="flex h-16 items-center justify-between px-4 md:px-6">
        <div class="flex items-center space-x-2">
            <div class="flex h-8 w-8 items-center justify-center rounded-lg">
                <img src="/bitacora2/logo.png" alt="Logo" class="h-8 w-8 hidden dark:block" />
                <img src="/bitacora2/logo.png" alt="Logo" class="h-8 w-8 block dark:hidden" />
            </div>
            {/*<span class="text-xl font-bold"></span>*/}
        </div>

        <nav class="hidden md:flex items-center space-x-8">
            {pages.map((page => (
                <a href={page.url} class="text-sm font-medium hover:text-blue-600 transition-colors">
                    {page.name}
                </a>
            )))}
        </nav>

        <div className="flex items-center space-x-4">
            <button 
                id="menu" 
                aria-label="Toggle Menu"
                class="px-2 py-1 ml-auto text-gray-500 rounded-md md:hidden focus:text-blue-500 focus:outline-none dark:text-gray-300"
            >
                <svg
                    class="w-6 h-6 fill-current"
                    xmlns="http://www.w3.org/2000/svg"
                    viewBox="0 0 24 24"
                >
                    <path class="navmenu-toggle" fill-rule="evenodd" d="M4 5h16a1 1 0 0 1 0 2H4a1 1 0 1 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2zm0 6h16a1 1 0 0 1 0 2H4a1 1 0 0 1 0-2z"></path>
                    <path class="navmenu-toggle hidden" fill-rule="evenodd" clip-rule="evenodd" d="M18.278 16.864a1 1 0 0 1-1.414 1.414l-4.829-4.828-4.828 4.828a1 1 0 0 1-1.414-1.414l4.828-4.829-4.828-4.828a1 1 0 0 1 1.414-1.414l4.829 4.828 4.828-4.828a1 1 0 1 1 1.414 1.414l-4.828 4.829 4.828 4.828z"></path>
                </svg>
            </button>
        </div>
    </div>
    <div class="navmenu-toggle hidden">
      <div class="flex flex-col justify-start order-2 w-full md:hidden">
        {pages.map((page => (
            <a href={page.url} class="px-5 py-2 text-sm font-medium text-gray-600 dark:text-gray-400 hover:text-blue-500">
                {page.name}
            </a>
        )))}
      </div>
    </div>

</header>
<script is:inline>
  const menuButton = document.getElementById("menu");
  menuButton.addEventListener("click", () => {
    [...document.querySelectorAll(".navmenu-toggle")].forEach((Element) => {
      Element.classList.toggle("hidden");
    });
  });
</script>
